diff --git a/C:\Users\hp\.claude\plans\read-docs-deeply-fizzy-planet.md "b/C:\\Users\\hp\\.claude\\plans\\read-docs-deeply-fizzy-planet.md"
index 0f1017e..3675985 100644
--- a/C:\Users\hp\.claude\plans\read-docs-deeply-fizzy-planet.md
+++ "b/C:\\Users\\hp\\.claude\\plans\\read-docs-deeply-fizzy-planet.md"
@@ -1,91 +1,96 @@
-# ALKEVA — Project Onboarding, Fact-Check & Planning Docs
+# ALKEVA — Phase 1 Implementation Plan (Foundation)
 
 ## Context
 
-ALKEVA is a gold/precious-minerals trading platform for Ethiopia (client: Tekleweyni Berhe). The deliverable on **Thu 13 Aug 2026** is an investor/bank **demo** — real ledger, real KYC, real price feed, Chapa in sandbox. The repo is empty except `docs/` (4 files; `ALKEVA_Contract.md` is empty — contract not written yet, known gap).
+Planning docs are complete and Phase 0 is fully done (GitHub repo, Render account with paid instances OK, Supabase project, Chapa test keys + Gemini key all exist). Phase 1 (per `docs/Phase_Plan.md`) builds the deployed skeleton: monorepo, full DB schema, auth, the price worker, and the first production deploy. **Done when: on a real URL, a user registers, logs in, and watches the live gold price in birr update every ~30s.**
 
-**Today is Wed 6 Aug.** The spec's schedule started Mon 3 Aug and assumed Milestone 1 done by Wed 5 Aug — **we are ~2 working days behind before writing a line of code**. The deadline stays 13 Aug (user confirmed), so the phase plan must compress 8 planned working days into ~5.5 actual ones. This is the single most important planning fact.
+New decision this plan: **Drizzle ORM** + drizzle-kit for schema/migrations (TS-first, native bigint, raw-SQL migration files for the append-only trigger).
 
-User-confirmed inputs:
-- Deadline: **13 Aug 2026** (spec wins over "10 days").
-- The 3 Aug meeting happened; blocking answers A1–A6 (fee %, sell-back reserve, tier structure, AI advise-vs-explain, equity) **are known** → collect them in the grill-me session.
-- Fact-check scope: **technical claims only** — ignore legal/regulatory claims.
-- Contract file: skip; not yet drafted.
+References: `docs/System_Design_and_Architecture.md` (authoritative for schema §3, spine §4, prices §6), `docs/Technical_FactCheck.md` §7 (feed choices), `CLAUDE.md` (rules: money integers, no coded tests, log progress).
 
-User's working rules (must be encoded into CLAUDE.md so they persist):
-1. **Never guess external APIs** — for Chapa (and any un-indexed docs), ask the user for docs/context before integrating.
-2. **No coded tests** (no unit/e2e suites). After each feature: I verify it manually myself, then hand the user written manual-testing steps.
-3. Track every change/progress in CLAUDE.md with date/time.
+## 1. Monorepo scaffold
 
-This task produces **documents only** — no application code. Coding starts after these docs are approved.
+pnpm workspaces, Node 22, TypeScript strict everywhere.
 
-## Deliverables (in order)
+```
+alkeva/
+├── apps/
+│   ├── web/        # Next.js 15 App Router + Tailwind + shadcn/ui + next-intl
+│   ├── api/        # NestJS 10
+│   └── worker/     # plain Node (tsx) price-tick loop
+├── packages/
+│   ├── db/         # Drizzle schema, migrations, client factory, seed script
+│   └── shared/     # zod config schema, DTO types, shared constants
+├── docker-compose.yml   # local Postgres 16 + Redis 7
+├── render.yaml          # Render blueprint: web, api, worker, key-value (Redis)
+└── .env.example         # every env key, documented
+```
 
-### 1. Technical fact-check pass → `docs/Technical_FactCheck.md`
+## 2. Database (packages/db) — the whole schema lands now
 
-Re-verify the technical claims made in `docs/Convo_With_Claude.ai.md` and `docs/ALKEVA_Spec_v1.0.md` (skip all legal/regulatory points). Use WebSearch/firecrawl for anything post-cutoff or uncertain; for Chapa specifics, **ask the user for docs rather than guessing**. Claims to verify:
+All tables from Design §3 in one initial migration set (schema lands once, complete — later phases add no migrations unless a defect forces one):
 
-- **Gemini Amharic/Tigrigna/Afaan Oromo quality** — client selected Gemini (Q45); Q39 lists 5 launch languages. Verify current Gemini multilingual support + free-tier limits.
-- **Chapa**: does it actually aggregate Telebirr and CBE Birr (spec C7's core justification)? Sandbox availability, webhook signature scheme, payout/transfer API for withdrawals. → flag as "needs user-provided docs" where the public web is thin.
-- **Gold/platinum price APIs** (GoldAPI, Metals-API, alternatives): free-tier rate limits vs a 30s tick worker, platinum support, USD→ETB rate source.
-- **Money-math rules**: integers only (ETB cents as bigint, gold milligrams as bigint) — confirm the schema approach and where rounding happens (fee calc, gram↔birr conversion).
-- **Ledger claims**: double-entry append-only with balance-as-projection; SERIALIZABLE vs row-locking claim; quote-TTL-in-Redis pattern; idempotency keys.
-- **Stack sanity for a solo 5.5-day build**: the spec prescribes Next.js + NestJS + FastAPI/LangGraph + Postgres + Redis — three runtimes. Assess whether the FastAPI/LangGraph sidecar earns its place vs calling Gemini from the Node backend directly (this becomes a grill-me question, with my recommendation).
-- **Three.js 3D visualisation** effort vs animated 2D fallback (spec cut-list item 6).
-- **Trust Wallet–style mobile web UI** with shadcn/ui + Amharic-first i18n — feasibility notes.
-- **Day-plan feasibility**: re-score the claude.ai 8-day plan against 5.5 remaining days; produce a corrected cut-list ordering.
+- **Ledger core:** `account` (with partial unique indexes for user vs system accounts), `ledger_transaction`, `ledger_entry`
+- **Trading:** `price_tick`, `quote`, `order` (unique `idempotency_key`), fee/`treasury_config`/`holding_tier_config` as config tables seeded from env
+- **Money in/out:** `payment` (unique `chapa_tx_ref`), `payout`
+- **Inventory:** `vault_holding`
+- **Identity/compliance:** `user`, `kyc_submission`, `compliance_event`, `freeze`, `audit_log`, `delivery_request`
+- **Misc:** `badge`, `user_badge`, `ai_conversation`, `ai_message`, `notification`
 
-Output format: table of claim → verdict (Confirmed / Wrong / Unverifiable-needs-user-input) → evidence → impact on the build.
+Money columns `bigint` (ETB cents, metal milligrams) per CLAUDE.md rule 4. Custom SQL migration adds: **append-only trigger on `ledger_entry` and `audit_log`** (reject UPDATE/DELETE), and a deferred constraint-trigger enforcing zero-sum per (transaction, asset).
 
-### 2. Grill-me session (Skill: `grill-me`)
+Seed script (`packages/db/seed.ts`): system accounts (`system:cash`, `system:vault:XAU`, `system:vault:XPT`, `system:fees`, `system:tax`, `system:reforestation`, `system:external`), config rows from env defaults, one admin user, initial `vault_holding` intake (demo-labeled). Idempotent (upserts).
 
-Run after the fact-check so questions are informed. Must collect:
-- **A1** exact commission/service-fee percentages (fee engine input)
-- **A2** dedicated sell-back reserve number (treasury halt threshold)
-- **A3** tier structure semantics (Q32 mixes holding limits with gemstone tier names — what does a "Tanzanite tier" actually gate?)
-- **A4** final AI scope: explain-only confirmed, or did the client reopen advice?
-- Architecture decisions: single Node backend vs NestJS+FastAPI split; monorepo layout; hosting target (which cloud, one region); price API choice + tick interval; Chapa docs handover; Gemini API key ownership; seed/demo data strategy; Amharic translation workflow (who reviews machine output).
-- Demo-day script priorities (Q67's five points) — what must be bulletproof vs merely present.
+## 3. API (apps/api)
 
-### 3. `CLAUDE.md` at repo root (the /init deliverable)
+NestJS modules this phase: `config` (zod-validated env), `db` (Drizzle provider), `auth`, `users`, `prices`, `health`.
 
-Structured as a living project file:
-- Project one-liner, deadline, non-negotiables (the 5 rules: AI never writes; balances never edited; quotes expire; maker-checker; never sell unowned grams).
-- Stack + commands (filled in once scaffolding exists).
-- **Working rules** (the user's 3 rules above, verbatim intent).
-- **Progress log**: append-only, `YYYY-MM-DD HH:MM — what changed — status` entries; every working session appends here.
-- **Decision log**: date-stamped record of grill-me answers and any later scope decisions.
-- Pointers to the other docs (spec, fact-check, system design, phase plan).
+- **Auth:** register (email+password, argon2id), login, refresh (rotating refresh token in httpOnly cookie; access JWT 15 min), `GET /me`, logout, recovery **stub** (endpoint accepts email, logs intent — full email flow later). Role enum (`user`, `administrator`, `compliance`, `finance`) + roles guard built now (used from Phase 5), default `user`.
+- **Prices:** `GET /prices/latest?asset=XAU|XPT` (latest tick + stale flag), `GET /prices/history?asset&range=24h|7d|30d|1y` (bucketed aggregates).
+- **Health:** `GET /healthz` — DB ping, Redis ping, latest-tick age (warn >3 min).
+- Global: validation pipe, rate limiting (Redis), CORS locked to web origin.
 
-### 4. `docs/System_Design_and_Architecture.md`
+## 4. Price worker (apps/worker)
 
-Rebuilt from scratch (not copied from claude.ai; every element re-justified, corrected by the fact-check + grill-me outcomes):
-- System context diagram and service layout (final, post-decision — e.g. if FastAPI sidecar is dropped, it's not in this doc).
-- Data model (the ~19 entities in spec §7, with the money-integer rules and system-account design).
-- The trading spine: price tick → quote → order FSM → ledger entry → balance projection.
-- Treasury/reserve-gate/float mechanics with the real A2 number.
-- AI sidecar contract: read-only tools, guardrail prompt, freeze-explanation flow.
-- Compliance rules engine (Q55 triggers, ETB 500k review, freeze/unfreeze roles).
-- Auth/KYC tiers, roles matrix (3 staff roles per Q47).
-- Deploy topology (Day-1 deploy rule), env/secret inventory, third-party dependency list.
+30s loop per Design §6: fetch gold-api.com (XAU, XPT USD) → on failure/timeout Swissquote public feed → USD→ETB from open.er-api.com cached 1h in Redis → compute `etb_per_mg` (integer micro-precision, rounding documented) → insert `price_tick` with `source`/`fx_source`. Both feeds fail → no insert (API serves last tick flagged stale). Graceful shutdown, single-flight (no overlapping ticks).
 
-### 5. `docs/Phase_Plan.md`
+## 5. Web (apps/web) — minimal by design
 
-High-level phase-by-phase division (explicitly NOT a technical implementation plan): sections/sub-sections from start to finish, re-planned for **Wed 6 → Wed 13 Aug** (not the spec's stale Mon-3-Aug calendar). Per phase: goal, sub-sections, "done when", what the user manually tests (with steps to be delivered per feature), and the milestone/payment mapping (M2 payment was due Fri 7 Aug per spec — flag that the payment calendar needs re-anchoring too). Include the corrected cut-list (what drops first if behind) and the never-cut four (ledger correctness, reserve gate, quote expiry, audit log).
+Phase 1 UI is deliberately thin (Phase-Plan rule: no UI investment beyond a button and a number until money core works):
+
+- next-intl scaffolding: `am` (default) + `en`, locale switcher, **Noto Sans Ethiopic** via `next/font`; all Phase-1 strings already going through message files
+- Pages: register, login, and a bare dashboard showing live XAU/XPT price in ETB (auto-refresh ~30s) + auth state
+- Auth wiring: cookie-based refresh, authenticated fetch helper in one place (reused all later phases)
+
+## 6. Deploy (Render + Supabase)
+
+- `render.yaml` blueprint: `alkeva-web` (paid), `alkeva-api` (paid), `alkeva-worker` (background worker), Render Key Value (Redis). Env groups per `.env.example`.
+- DB: Supabase `DATABASE_URL` (session pooler string); `drizzle-kit migrate` + seed run as api pre-deploy command.
+- Keep portable (future VPS/cPanel): everything also runs via `docker-compose` + `pnpm start`, no Render-specific code.
+- **Dagmfre does the dashboard steps** (connect repo, create blueprint, paste env values) from written instructions I provide — secrets never enter the chat.
+
+Env keys (`.env.example`): `DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `JWT_REFRESH_SECRET`, `WEB_ORIGIN`, `FEE_COMMISSION_PCT=2.0`, `FEE_SERVICE_CENTS=0`, `TAX_PCT=0`, `REFOREST_PCT=0`, `TREASURY_FLOAT_ETB_CENTS`, `TREASURY_HALT_CENTS`, `SELLBACK_DAILY_CEILING_CENTS`, `TIER_BANDS_JSON`, `PRICE_TICK_SECONDS=30`, (Chapa/Gemini keys reserved for Phases 4–5).
 
 ## Execution order
 
-1. Fact-check research (WebSearch/firecrawl; ask user for Chapa docs) → write `Technical_FactCheck.md`
-2. `/grill-me` session with the user (collect A1–A4 + architecture decisions)
-3. Write `CLAUDE.md` (seeded with decisions + first progress-log entry)
-4. Write `System_Design_and_Architecture.md`
-5. Write `Phase_Plan.md`
-6. Present all docs; per the user's global preference, end with the changed-files table.
+1. Scaffold monorepo + docker-compose; commit
+2. packages/db: schema, trigger migration, seed; verify against local Postgres
+3. api: config/db/health → auth → prices
+4. worker: tick loop; verify ticks flowing locally, test feed-fallback by blocking primary
+5. web: i18n scaffold, register/login, live-price dashboard
+6. render.yaml + deploy instructions → Dagmfre connects → production verification
+7. Update CLAUDE.md progress log; write manual test steps
 
 ## Verification
 
-- Fact-check doc: every claim row has a verdict and a source (or an explicit "needs user docs" flag) — no unresolved "probably" rows.
-- CLAUDE.md: contains the 3 working rules, the 5 non-negotiables, and a dated first progress entry.
-- System design doc: contains zero features marked OUT OF SCOPE in spec §5.4, and reflects grill-me decisions (not claude.ai defaults).
-- Phase plan: dates run Aug 6→13, total effort ≤ 5.5 working days, cut-list present, no legal-precondition tasks assigned to the developer.
-- User reviews all three docs before any code is written.
+**Self-verify (me):** fresh `docker-compose up` → migrate+seed → register/login/refresh via API; ledger trigger test (manual UPDATE on ledger_entry must fail); worker writes ticks every 30s; kill primary feed (bad DNS env) → Swissquote fallback ticks; `/healthz` green; web register→login→live price in both locales.
+
+**Manual test steps for Dagmfre (delivered as `docs/manual-tests/phase-1.md`):**
+1. Local: `docker-compose up`, `pnpm db:migrate && pnpm db:seed`, `pnpm dev` → register on `localhost`, log in, watch price change twice (~60s), switch Amharic↔English, confirm Ethiopic font renders
+2. Refresh flow: stay idle >15 min → still authenticated after action (silent refresh)
+3. Wrong password → clear error; duplicate email → clear error (in Amharic by default)
+4. `GET /healthz` shows db/redis/price OK
+5. Production: repeat steps 1-item flows on the Render URL after deploy
+6. Confirm the price shown roughly matches world gold price × USD→ETB rate (sanity arithmetic)
+
+Bugs found → fix loop per CLAUDE.md build loop → then `/handoff`.