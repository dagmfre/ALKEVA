# ALKEVA — Project Guide & Progress Tracker

**One-liner:** Gold/platinum digital trading platform for Ethiopia — the platform is the counterparty (dealer, not broker), with a double-entry ledger, Chapa payments, Gemini AI assistant (Amharic-first), and an admin console. Deliverable: **investor + bank demo on Thu 13 Aug 2026**, real code, Chapa in sandbox.

**Client:** Tekleweyni Berhe (sole owner) · day-to-day contact: Goitom Hadush · Developer: Dagmfre Seid

## Documents

| Doc | Purpose |
|---|---|
| `docs/ALKEVA_Spec_v1.0.md` | Signed-scope spec: feature register F1–F31, constraints C1–C10, cut list §5.4 |
| `docs/Technical_FactCheck.md` | Verified/corrected technical claims (6 Aug 2026) — read before touching ledger, Chapa, Gemini, or price feed |
| `docs/System_Design_and_Architecture.md` | Final architecture, post fact-check + decisions |
| `docs/Phase_Plan.md` | Phase-by-phase division, re-anchored Aug 6→13 |
| `docs/Gold Platform — Client Discovery.md` | Client's raw form answers (Q1–Q80) |
| `docs/Convo_With_Claude.ai.md` | Original exploration — superseded where it conflicts with the fact-check |
| `docs/ALKEVA_Contract.md` | **Empty — contract not yet written** (known gap) |
| `design/` | Design brief: brand · design (tokens) · screens · antipatterns + preview cards. Pushed to claude.ai/design → **ALKEVA Design System** (`d4c89daf…`). **The approved comp the UI is built from is project `9788efcb…` (`ALKEVA Screens.dc.html`)** — 1440 frames + 390 variants for Home/Trade/Portfolio/Receipt + Foundations. (The earlier `569b08d6…` / `ALKEVA P0.dc.html` was the failed prose-document deliverable — superseded.) |
| `docs/manual-tests/phase-3.md` | Phase 3 joint verification — 53 steps, sections A–M, plus **section N: the demo run sheet** |

## The five non-negotiables

1. **The AI never writes.** Read-only tools only. It cannot trade, approve, freeze, or reallocate.
2. **Balances are never edited.** Every change is a pair of immutable ledger entries; balance is a projection.
3. **Quotes expire.** Users confirm against a quote ID, never a live price.
4. **No one moves money alone.** Privileged actions require a second approver (demo: single-step allowed on cut list, audit-logged always).
5. **Never sell a gram the vault doesn't hold.** Reserve gate enforced *inside* the buy transaction. No admin override.

## The per-phase build loop (agreed 6 Aug 2026)

Every phase in `docs/Phase_Plan.md` runs this exact loop:

1. **Plan** — Claude prepares an implementation plan for the phase **in plan mode**; Dagmfre approves it.
2. **Build** — Claude implements the phase.
3. **Self-verify** — Claude verifies its own implementation end-to-end (running code, checking flows — no coded tests).
4. **Joint verification** — Claude hands Dagmfre **step-by-step manual testing instructions**; Dagmfre runs the app and tests each feature himself.
5. **Fix loop** — bugs found in 3–4 are fixed and re-verified; repeat until both are satisfied with the phase.
6. **Handoff** — Claude runs the `/handoff` skill to produce a handoff document; Dagmfre compacts the session; the next phase starts fresh from CLAUDE.md + the handoff.

Rules inside the loop: progress/decisions are logged here as they happen (not at handoff time); a phase is never declared done while a known bug is open; the next phase's plan starts only after the previous handoff.

## Working rules (how this project is built)

1. **Never guess an external API.** For Chapa or any un-indexed/uncertain external docs: ask Dagmfre for docs/context first. (Chapa endpoints verified 6 Aug against developer.chapa.co — see fact-check §6; two items still need live test keys: wallet `bank_code` values, transfers-in-test-mode.)
2. **No coded tests.** No unit/e2e suites. After implementing each feature: verify it manually end-to-end, then hand Dagmfre written manual-testing steps so he can independently confirm the feature is healthy.
3. **Track everything here.** Every working session appends to the Progress Log below with date/time. Every decision goes in the Decision Log. This file is the single source of truth for project state.
4. **Money is integers.** ETB in cents (`bigint`), metals in milligrams (`bigint`). Rounding happens exactly once, in the quote engine.
5. **Cut-list discipline.** When behind, drop from the cut list in `docs/Phase_Plan.md` — never from the five non-negotiables.

## Stack (decided 6 Aug 2026)

| Layer | Choice | Notes |
|---|---|---|
| Frontend | Next.js 15 + Tailwind 4 + **shadcn/ui (installed 9 Aug, fully re-tokenised)** | **Desktop-first responsive website** (not a native app): 1440 workspace primary, 390 phone variant. Amharic default (`next-intl`, Noto Sans Ethiopic), dark-only. Design system: `design/` + claude.ai/design |
| API | NestJS (TypeScript) | Ledger, trading, treasury, compliance, admin, **and** the Gemini chat endpoint |
| AI | `@google/genai` JS SDK → Gemini 3.6 Flash (demo) / 3.5 Flash-Lite (rehearsal) | **No Python/FastAPI/LangGraph** — dropped 6 Aug |
| DB | PostgreSQL on **Supabase** | Used as plain managed Postgres (connection string); auth is our own JWT |
| Cache | Redis (Render Key Value) | Idempotency keys, rate limiting. Quotes live in Postgres, not Redis |
| Payments | Chapa — hosted checkout + webhooks + transfers API | Sandbox (`CHAPUBK_TEST`/`CHASECK_TEST`) until merchant account signed |
| Price feed | gold-api.com primary, Swissquote public feed fallback, 30s tick | Production later: metals.dev (~$20–40/mo, native ETB) |
| FX | open.er-api.com USD→ETB, cached hourly | Market rate (not NBE indicative). Rate + source stored on every `price_tick` |
| 3D | Three.js / react-three-fiber | Built late; 2D animated fallback ready |
| Hosting | **Render** (web + api + worker) + Supabase | Paid instances for demo (free tier cold-starts). Keep portable for future VPS/cPanel |

## Decision Log

| Date | Decision | Detail |
|---|---|---|
| 2026-08-06 | **A4 — AI scope: explain + alerts only** | Explains prices/history/fees/own position; non-advisory price alerts. Never recommends, never acts. (Spec §4.1 resolved.) |
| 2026-08-06 | **A1 — Fees: fully config-driven** | Commission as env/config (e.g. `FEE_COMMISSION_PCT`, ~1.5–2%); service fee, tax, reforestation as config keys defaulting to 0. No hardcoded rates. |
| 2026-08-06 | **A2 — Treasury: config-driven** | `TREASURY_FLOAT_ETB` + halt threshold as config, seeded with labeled demo values. Real numbers are a client decision, still open. |
| 2026-08-06 | **A3 — Tiers: status labels + limits** | Gemstone tiers computed from holding value (ETB, USD reference); gate per-txn/daily caps + delivery eligibility. Separate from KYC tiers. Bands config-driven. |
| 2026-08-06 | **Backend: Next.js + NestJS, no Python** | Gemini via JS SDK inside NestJS (fact-check §3.1, §5.3). Saves ~1 day. |
| 2026-08-06 | **Hosting: Render + Supabase** | Future migration to VPS/cPanel possible — no platform lock-in code. |
| 2026-08-06 | **Price: free keyless feeds + market FX** | $0 demo. gold-api.com + Swissquote fallback; open.er-api.com FX. metals.dev for production. |
| 2026-08-06 | **Credentials: Dagmfre creates Chapa test + Gemini keys today** | Swap to client-owned before launch. |
| 2026-08-06 | **Amharic: Dagmfre reviews, client spot-checks ~day 5** | Machine-translate → developer review per batch → one client pass on final UI. |
| 2026-08-06 | **Demo spine: full money loop, live on stage** | register → KYC → deposit (sandbox) → buy → portfolio/3D → sell → withdraw → admin approve → AI explains in Amharic. Seeded accounts carry history/badges/freeze/treasury panels. |
| 2026-08-06 | **Languages for 13 Aug: Amharic + English only** | Tigrigna/Afaan Oromo/Somali not demo-quality on Gemini (fact-check §5.1) — positioned as post-demo. |
| 2026-08-06 | **Ledger mechanics** | Row locks (`SELECT … FOR UPDATE`), not SERIALIZABLE; quotes in Postgres with `expires_at`; reserve gate inside buy transaction (fact-check §2). |
| 2026-08-09 | **Component foundation: shadcn/ui** (Dagmfre) | Multi-page system — dashboards, price chart, many components — needs a real foundation, not hand-rolled primitives. shadcn base **fully re-tokenised**; its default slate look never ships. Tailwind 4 + React 19. |
| 2026-08-09 | **Theme: dark only for the demo** | Gold-on-dark, one theme to polish in the remaining days. Light mode is post-demo. |
| 2026-08-09 | **Design authored via claude.ai/design** | Brief bundle lives in `design/` (brand · design · screens · antipatterns + 5 preview cards), pushed with the DesignSync tool to project **ALKEVA Design System**. Loop: brief → push → Dagmfre reviews → iterate → **UI build gated on his approval**. |
| 2026-08-09 | **Design position: custody instrument, not luxury** | Gold is the commodity, not the decoration. Achromatic dark ink; the only saturated colours are the two metals, the primary action, and state. Rejects both the luxury-vault reflex (gold gradients, serif, shimmer) and the terminal-fintech reflex (monospace, green-on-black grids). |
| 2026-08-09 | **No amber/yellow in the palette** | Would collide with brand gold and dilute the one signal meaning "asset / action". Caution states use the cool platinum family; critical uses the destructive family. |
| 2026-08-09 | **Desktop-first responsive — the 480px-centred mandate is dead** (Dagmfre) | Root cause of the Phase 3 UI failure: `design.md` §6 mandated "desktop shows the phone layout centred" and both claude-design and the build faithfully executed it. Revised: primary composition is a 1440px workspace (sidebar + top bar + panel grid); every screen also ships a 390px mobile variant (bottom tab bar). Structure reference: `design/previews/perfect ui structure….jpg`. |
| 2026-08-09 | **Richer gold branding** (Dagmfre) — revises the austere position | Warm gold-tinted surfaces (chroma 0.008–0.010 @ hue 85) + glossy gradient gold CTA (top `oklch(0.82 0.155 88)` → `#d4a017` → `#b8860b`), per `design/previews/goodl coloring….jpg`. Overrides the earlier "no gradients / achromatic ink" bans; still banned: gradient text, shimmer, glow, white-on-gold, celebration. All 18 contrast pairs recomputed and pass (worst: dark ink on gradient bottom stop 5.96:1). |
| 2026-08-10 | **Approved comp is the build contract** | claude-design delivered `ALKEVA Screens.dc.html` (project `9788efcb…`): full 1440×900 frames + 390 variants for Home, Trade, Portfolio, Receipt, plus a rendered Foundations specimen. It adopted the ramp-v2 token set verbatim, so the tokens shipped unchanged and the build follows the comp's composition, not a re-interpretation of it. |
| 2026-08-10 | **Trade is a route on desktop, a sheet on the phone** | Same `useTradeForm` state machine both ways. A sheet exists so the price behind it stays visible on a small screen; on a desk there is room to keep the live chart beside the ticket, which is the point of the wider layout. `useIsDesktop` gates only *behaviour*, never layout — layout is CSS. |
| 2026-08-10 | **Two new destinations: `/trade` and `/receipts`** | The comp's rail has six items. Receipts is a real page (settled orders by serial) rather than a filter on History, because the serial is allocated at settle and is the answer to "is there a receipt?". |
| 2026-08-10 | **Clocks and receipt timestamps are pinned to EAT** | `eatStamp()` formats in `Africa/Addis_Ababa` in both locales. A receipt whose hour changes with the reader's laptop timezone is not a record. |
| 2026-08-09 | **Fix path for the two design failures: design loop first** (Dagmfre) | Brief rewritten + re-pushed with an explicit artifact contract ("one full 1440px frame + 390px variant per screen; prose-with-fragments is a failed deliverable" — claude-design had shipped a design-rationale document + a blank file). Dagmfre re-runs claude-design and approves full desktop comps **before** the UI rebuild starts. |
| 2026-08-11 | **Phase 4+5 scope** (Dagmfre, AskUserQuestion) | Passkey/WebAuthn **cut** (cut-list #6); notifications = **in-app records + real email via free SMTP** (Brevo/Gmail app password, provider-agnostic nodemailer, unset = degrade to records-only); compliance stays **500k→review + manual freeze** (cut-list #7); price alerts **built** (F24). |
| 2026-08-11 | **Chapa sandbox transfers confirmed** | `POST /v1/transfers` accepts a test-mode-only `status: success\|failed\|pending` field forcing the outcome (developer.chapa.co/transfer/transfers) — resolves fact-check §6.4's open question. Approve path forces `success` in test mode for a deterministic demo; verify still gates settlement. |
| 2026-08-11 | **Webhook = trigger, never state** | The webhook handler (and the return page) only ever *prompt* a server-side `verify` against Chapa's API; `settleByTxRef` is the single credit path, idempotent via a conditional UPDATE mutex. Both documented signature headers accepted (`chapa-signature` = HMAC(secret,secret), `x-chapa-signature` = HMAC(secret, raw body)), timing-safe, over `req.rawBody`. |
| 2026-08-11 | **Withdrawal = ledger hold in `system:payout_hold`** | Request moves user ETB → hold (insert-first idempotency, same pattern as orders); finance approve → Chapa transfer → verified settle moves hold → external; reject/definitive-failure refunds hold → user. **Ambiguous transfer errors (network/timeout) never refund** — the transfer may have queued; stay `processing` until verify/webhook resolves. Approving your own payout is forbidden even in the demo. |
| 2026-08-11 | **KYC gates deposit, withdrawal, AND trading at tier ≥1** | Spec 4.3. New rejection code `kyc_required`; docs stored in-row (bytea ≤2 MB, no object storage provisioned); migration 0004 grandfathers pre-gate users to tier 1 (one-time, unlike the every-deploy seed). |
| 2026-08-13 | **One live price platform-wide: SSE push + shared client store** (Dagmfre) | Worker publishes `price:tick` on Redis after each insert → API `PriceFeedService` (debounced snapshot) → `@Sse /prices/stream` → web `PriceProvider` (one EventSource for the whole app, 10s-poll fallback, visibility pause, staleness ratchet). "Current" is ALWAYS the latest raw tick — `usePriceSeries` no longer exposes `current`/`changePct`, so a bucket average can never again render as the live price. Canonical 24h change computed API-side (`change24hPctMilli` on `/prices/latest`) — ticker, cards, and the AI quote the same number. Portfolio re-marks value client-side from the store (cost basis stays server-authoritative). Honest limit: the upstream free feed ticks ~30s; the guarantee is one identical value everywhere, pushed ~1s after a tick lands; money movement stays protected by quote expiry. |
| 2026-08-13 | **Withdrawals: instant below threshold** (Dagmfre, AskUserQuestion) | `PAYOUT_AUTO_APPROVE_MAX_CENTS` (default 500k ETB, = the AML line; 0 = all-manual): strictly below, request-time auto-approve fires the transfer as actor `system:auto_approve` (the audit-logged single-step cut-list variant); at/above — or Chapa unconfigured, or any instant-path failure — the payout stays `requested` for finance. Hold mechanism, ambiguity-never-refunds, settle mutex, webhook re-verify, human self-approval ban all unchanged. |
| 2026-08-13 | **Administrator is a superuser** (Dagmfre, AskUserQuestion) | `administrator` added to every compliance-/finance-scoped `@RequireRoles` + the admin nav/screens. Compliance and finance stay scoped; payout self-approval ban binds administrators too. (The "admin can't see KYC" report was this role separation working as designed.) |
| 2026-08-13 | **AI assistant: hardened stateless replay — chaining rejected** | Kept `store:false` + full replay from `ai_message` (long-term history lives in OUR db forever; `previous_interaction_id` would demand `store:true` = user balances in Google-side retention, and free-tier 1-day expiry means the replay path must exist anyway). Hardened: error taxonomy (Gemini 429 → HTTP 429 `ai_rate_limited`; invalid replay → one retry with text-only history, so a poisoned row can never brick a conversation; transient → one 1.5s retry; else `ai_unavailable`), per-step sanitation, steps replayed only for the newest 6 assistant rows, context 30→60 rows under a 50k-char budget. UI: failed sends become retryable bubbles (no orphans), New-conversation button, 429s localized. Streaming deliberately deferred (POST endpoint, tool rounds dominate wall-time). |
| 2026-08-13 | **Chapa sandbox findings (live-verified)** | (1) Transfer `reference` must be ≤36 chars — `ALKPO`+uuid was 37, so EVERY real transfer failed; now `ALK`+uuid (35). (2) Test-mode `/v1/transfers/verify` returns `data: [null]` — no per-transfer status exists in sandbox, so verify could never settle; an envelope-level success in test mode is now accepted as the confirmation (`sandbox_transfer_verified` / `chapa_webhook_sandbox` audit vias; live keys never take the branch). Resolves fact-check §6.4 fully. (3) Chapa validation errors put an object in `message` — now stringified, never `[object Object]` in failureReason. |
| 2026-08-13 | **Admin analytics on recharts** | `GET /admin/analytics?days=30` (5 grouped aggregates, zero-filled through today) + recharts with a hand-rolled re-tokenised chart wrapper (`components/ui/chart.tsx`). Series colours are ONLY the two metals (gold/platinum pair CVD-validated: ΔE 17 deutan, 17.4 normal, ≥3:1 on card); identity never colour-alone (legend + tooltips). |

## Open items (client-owned)

- [ ] Real treasury float + halt numbers (A2) — config keys exist, values are placeholders
- [ ] Exact fee percentages confirmed before demo (A1) — currently env placeholder
- [ ] Chapa merchant account signed (live keys) — sandbox until then
- [ ] Contract + equity/revenue-share terms (spec §12) — `ALKEVA_Contract.md` still empty
- [ ] Licences/legal preconditions (spec §3) — client-owned, not software

## Progress Log

*Append-only. Format: `YYYY-MM-DD HH:MM — entry — status`.*

- 2026-08-06 21:45 — Read all docs deeply (spec, discovery form, claude.ai convo); found contract empty, timeline 2 days stale, blockers A1–A6 unrecorded — done
- 2026-08-06 22:15 — `docs/Technical_FactCheck.md` written: 8 sections, 3 live-research passes (Gemini, Chapa, price APIs). Key corrections: drop Python sidecar; GoldAPI/Metals-API rejected for free keyless feeds; Amharic+English only for demo; row-locks not SERIALIZABLE; Chapa sandbox confirmed viable — done
- 2026-08-06 22:37 — Grill session complete: A1–A4 resolved (all config-driven), stack/hosting/feed/credentials/Amharic/demo-spine decided — logged in Decision Log above — done
- 2026-08-06 22:40 — CLAUDE.md created (this file): non-negotiables, working rules, stack, decision log, open items — done
- 2026-08-06 22:50 — `docs/System_Design_and_Architecture.md` written: monorepo layout, data model, trading spine, Chapa flows, price/FX pipeline, compliance engine, AI contract, roles, deploy topology — done
- 2026-08-06 23:00 — `docs/Phase_Plan.md` written: Phases 0–7 re-anchored Thu 6→Thu 13 Aug (weekend worked), per-phase done-when + manual-test lists, cut list, payment re-anchoring flag. Weekday correction: today is **Thu** 6 Aug, ~4.5 business days (+weekend) remain — done
- 2026-08-06 23:00 — **Planning complete. Next session: Phase 0 (credentials + repo scaffold), pending Dagmfre's review of the three new docs** — ready
- 2026-08-06 23:10 — Per-phase build loop documented (plan → build → self-verify → joint manual verification → fix loop → /handoff → compact). Phase 1 plan approved (adds decision: **Drizzle ORM** for schema/migrations; Phase 0 confirmed fully done — repo, Render, Supabase, Chapa test keys, Gemini key all exist) — done
- 2026-08-07 00:15 — Monorepo scaffolded (pnpm workspaces: apps/web+api+worker, packages/db+shared, docker-compose, .env.example); `.claude/` untracked from git — done
- 2026-08-07 00:30 — packages/db complete: full 23-table Drizzle schema (bigint money), migration 0000, custom migration 0001 (append-only triggers on ledger_entry+audit_log, deferred zero-sum-per-asset constraint trigger, asset-match trigger), idempotent seed. **Trigger tests passed live**: unbalanced commit rejected, UPDATE/DELETE rejected, asset-mismatch rejected — done
- 2026-08-07 00:45 — NestJS API running (SWC ESM runtime): zod-validated env, argon2id auth with rotating refresh cookies, roles guard, /prices/latest+history, /healthz, throttling. Smoke-tested: register/me/refresh-rotation/logout, wrong-password 401, duplicate 409 — done
- 2026-08-07 00:55 — Price worker running: 30s tick, gold-api.com primary → Swissquote fallback (**fallback verified live** for XAU+XPT), hourly-cached FX (open.er-api.com), integer ETB-cents-per-gram math (sanity-checked vs spot×FX), staleness handling — done
- 2026-08-07 01:05 — Web app running: Next.js 15 + Tailwind 4, next-intl cookie-locale (Amharic default, Noto Sans Ethiopic), register/login, live dashboard (30s refresh, stale warning), /api rewrite proxy keeping cookies first-party (onrender.com is on the Public Suffix List — direct subdomain cookies would be cross-site). **Bug found+fixed in verification**: refresh cookie Path=/auth was stranded behind the proxy prefix → Path=/. Full cookie lifecycle re-verified through the proxy — done
- 2026-08-07 01:10 — render.yaml blueprint + `docs/deploy/render-setup.md` (Dagmfre's dashboard steps) + `docs/manual-tests/phase-1.md` (26-step joint verification). **Awaiting: Dagmfre runs deploy steps + manual tests; fix loop; then /handoff closes Phase 1** — ready for joint verification
- 2026-08-07 15:23 — /handoff run: handoff doc written to `%TEMP%\ALKEVA_Handoff_Phase1_to_Phase2.md`. **Note: joint-verification results (26 steps + Render deploy) were not reported in-session — next session must confirm them first; any failure enters the fix loop before the Phase 2 plan** — done
- 2026-08-07 23:00 — Render free-tier deploy fixes: render.yaml reworked (no preDeployCommand/worker service on free — migrate+seed+worker folded into api startCommand; upgrade path documented in render-setup.md); deploy failure diagnosed: DATABASE_URL pasted with quotes and/or transaction-pooler port 6543 → createDb now strips quotes and fails loudly on malformed URLs; **Supabase DB password appeared in session → Dagmfre should rotate it** — done
- 2026-08-08 00:05 — Phase 1 gate (Dagmfre): local steps passed; production (Render steps 24–26) pending behind the DATABASE_URL fix — folded into Phase 2 verification. Phase 2 plan approved (adds: self-serve demo faucet until Chapa, quote.reforest_cents migration 0002, seeded opening-float ledger txn, minimal 500k→review routing, tier caps via TIER_BANDS_JSON optional fields) — done
- 2026-08-08 01:15 — **Phase 2 built** (commits b4e19c3→c7a1f7f): LedgerService (id-ASC FOR UPDATE lock protocol, postTransaction, balance projection), fee engine (milli-pct), quote engine (single rounding point, 30s TTL, stale refusal), OrdersService (insert-first idempotency claim, quote lock, frozen/review/tier/balance/reserve/float/sellback gates, rejections COMMIT with machine-readable reason), treasury summary (derived reserve ratio + float), faucet, throwaway trade panel (am+en). **Self-verified live:** buy 5g settled + 5× parallel mash → same order id; sell 2g; balances reconcile to the cent; quote_expired / quote_consumed / insufficient_balance / reserve_halt (exact boundary: 2g halts, 1g=available settles) / float_halt / sellback_ceiling / account_frozen / 500k→review all fire correctly; zero-sum holds after everything; treasury numbers hand-verified. ZodPipe generics fix (transforming schemas); drizzle-kit can't serialize 0n defaults → sql`0` — done
- 2026-08-08 16:30 — **Phase 2 self-verification completed** (f9478d4): closed 5 never-executed paths found by auditing my own trace — `tier_txn_cap` + `tier_daily_cap` (caps ship NULL/uncapped so the branch had never run; verified both directions incl. the daily accumulator), `insufficient_metal`, idempotency-key **conflict** (same key + different quote → 409), faucet-disabled → 404, `GET /orders/:id`, `amount_too_small`. All 10 rejection reasons now have live rows; append-only trigger re-verified against Phase 2 writes (UPDATE + DELETE both rejected); zero-sum holds on all 3 assets. **Bug found + fixed:** `loadDotenvUpwards` documented "existing process.env keys win" but `process.loadEnvFile()` overwrites — a stray `.env` could shadow platform-injected secrets; now snapshots and restores injected vars (verified both directions). Manual-test doc extended to 30 steps/sections A–K (tier caps had no step despite being Phase 2 scope 2.4) — done
- 2026-08-09 — **Phase 3 plan approved** (design-first). Decisions taken: shadcn/ui foundation, dark-only, backend read endpoints built during the design review wait. Phase 3 runs as a design loop: `/impeccable` (read-only) → author brief → push to claude.ai/design → **Dagmfre reviews** → iterate → build — done
- 2026-08-09 — **Design brief bundle authored + pushed.** `/impeccable` read-only pass (register: product; craft rules + anti-slop lists distilled). `design/` written: `README.md` (the prompt for claude-design, P0–P2 deliverables, 10 hard constraints), `brand.md` (scene sentence, custody-not-luxury position, voice, 5 anti-references, 5 principles), `design.md` (full OKLCH token set in shadcn variable vocabulary **with measured contrast ratios**, bilingual type scale, motion, component inventory), `screens.md` (7 screens + system states, real API shapes and real Amharic strings, all 10 rejection treatments), `antipatterns.md` (8-section refusal list). 5 preview cards: foundations, trade-quote, home, portfolio, receipt. Pushed via DesignSync → project **ALKEVA Design System** (`d4c89daf-f7ea-4c39-be90-612f3da3311c`), 10 files + 5 registered cards. Design problems surfaced and solved in the brief: gold-vs-amber signal collision, the **"Gold" tier name colliding with the gold asset**, the **first-loss problem** (portfolio shows a loss equal to the commission right after a buy), demo reserve ratio being ~38× not ~1×, and Ethiopic script constraints (no uppercase, no letter-spacing, 15px floor, +0.2 line-height). **Awaiting Dagmfre's review — UI build is gated on it** — ready for review
- 2026-08-09 — **Design approved; Phase 3 UI built.** Imported the approved comp from claude.ai/design (project `569b08d6…`, `ALKEVA P0.dc.html` — it adopted the token set verbatim and supplied both locales' strings). shadcn/ui installed (`components.json` hand-written; the new CLI's interactive preset prompt can't be scripted) and **re-tokenised** — Button rebuilt for 44px minimum targets and dark-ink-on-destructive (stock shadcn ships 36px and `text-white`, both of which fail our rules), Card stripped of shadow and forced padding, Sheet moved to the popover surface with no backdrop blur. `globals.css` now carries the full OKLCH token set + the `:lang(am)` typography rules (no letter-spacing, 1.7 leading) + the `.tnum` utility. Built: app shell (header, 5-item bottom bar, middleware auth gate that checks the **refresh** cookie so a returning user isn't bounced), Home (balances, price cards, chart with 4 ranges, trust panel), the trade sheet (amount → quote → settled, countdown ring, all 17 rejection codes localized), Portfolio (cost basis, gain/loss, tier card), History (day-grouped divided list, reasons inline), Receipt (document treatment, serial, provenance), Account, and re-styled auth. Phase 2's throwaway `dashboard.tsx`/`trade-panel.tsx`/`locale-switcher.tsx` deleted. **Bug found + fixed during verification:** `countdown-ring.tsx` imported a *value* (`QUOTE_TTL_SECONDS`) from `@alkeva/shared`, which drags the Node-only `env.ts` into the browser bundle → every authenticated route 500'd. Every other web import from shared is `import type` (erased at compile time), which is why this never surfaced before. Fixed by deriving the ring's span from the quote the server actually issued — better design, since the ring now follows a server-side TTL change instead of trusting a client copy. **Verified live:** all 6 routes 200 in both locales with correct strings, `<html lang>` switches, signed-out → /login and signed-in → /login bounces to Home, receipt route renders; **money-core regression through the new Next proxy path**: buy settled, 5 parallel confirms → 1 order + 1 ledger txn, insufficient_balance / insufficient_metal / quote_expired all fire, zero-sum 0 on all 3 assets, no zero-amount entries, append-only trigger still rejects UPDATE, serials settled-only and unique. Whole-workspace typecheck clean; production build clean (8 routes, 134 kB first load). `docs/manual-tests/phase-3.md` written — 45 steps, A–L — done
- 2026-08-09 — **Accessibility + brand-fidelity audit** (`pnpm verify:contrast`, checker committed at `scripts/verify-contrast.mjs`). Converted every shipped OKLCH token → OKLab → linear sRGB → WCAG relative luminance and checked all 14 foreground/background pairs the UI actually uses. **All 14 pass**, comfortably (lowest is 5.01:1 for timestamps on card, floor 4.5). White-on-gold measures 2.38:1 — correctly banned, as the docs state. **Two real defects found and fixed:** (1) the contrast figures published in `design.md` were *estimates* presented as measured, off by up to 2.4 — all corrected to computed values, and the doc now states the method; (2) **`--gold-500` was not actually the client's brand gold** — the shipped `oklch(0.723 0.146 84)` renders `#d09c0e`, not `#d4a017`. Converted the client's hexes exactly (`#d4a017` = `oklch(0.735 0.146 84.3)`, `#b8860b` = `oklch(0.652 0.132 81.6)`, both verified to round-trip) and corrected the token in `globals.css` and across the design bundle; the exact value is confirmed present in the served CSS. Every `≈ hex` annotation in `design.md` was also wrong (e.g. `--background` documented as `#191816`, actually `#0b0a08`) — all regenerated. **Still not done: visual/touch verification** — the Chrome extension is not exposed to this session, so nothing has been seen rendered; `docs/manual-tests/phase-3.md` remains the gate — done
- 2026-08-09 — **The two Phase 3 design failures diagnosed + design brief rewritten and re-pushed.** Root cause found in writing: `design.md` §6 mandated "desktop shows the phone layout centred — this is a mobile product" and README repeated it; claude-design and the build both faithfully executed the wrong instruction. Second failure: claude-design shipped a design-rationale document (prose + UI fragments) plus a blank file instead of full screens. Fixes (decisions logged above): README rewritten with an explicit **artifact contract** + desktop-first framing + the two inspiration images named (structure ← Tradeo jpg, coloring ← Gauld jpg); `design.md` §6 rewritten as a responsive system (1440 sidebar+topbar+grid / 390 bottom-tab variant); richer-gold tokens (warm surfaces chroma 0.008–0.010 @ hue 85, gradient CTA `#f6c835 → #d4a017 → #b8860b`, gloss = inner highlight never outer glow) — **all 18 contrast pairs recomputed, all pass** (worst 5.96:1, white-on-gradient-top 1.59:1 stays banned); `antipatterns.md`/`brand.md` revised to the sanctioned-gradients position. **Two new 1440×900 preview cards** (`desktop-dashboard.html`, `desktop-trade.html`) authored at Tradeo detail density (ticker top bar, labeled y-axis with gold current-price tag, open/high/low readout, time axis, field-styled inputs with MAX + quick chips, inset fee-summary box, tier chip pinned in sidebar) and **render-verified in Chrome**; the 5 phone previews retinted to the warm tokens and demoted to group "Mobile variant (390)". Whole bundle (14 files incl. both jpgs) pushed via DesignSync to ALKEVA Design System. **UI rebuild (Part C) gated on Dagmfre approving new claude-design comps.** — done
- 2026-08-09 — **B1+B2 while awaiting design review.** B1: the four browser-found fixes committed (1cf89e7) after clean whole-workspace typecheck. B2: trade state machine extracted to `use-trade-form.ts` (idempotency key, server-TTL countdown, non-disabled confirm, settled-on-refusal); sheet is now presentation-only and the desktop trade page will mount the same hook; error state now stores machine codes not translated strings (platform-halt check was comparing translated text). Typecheck clean. **Live verification through the refactored hook reached the quote stage** (sheet → live balances/prices → quote issued: 1g @ 22,541.26 + 450.82 commission = 22,992.08, ring at 23s, provenance line) before Dagmfre called time on browser testing; confirm→receipt not re-driven this session — covered by `phase-3.md` E/F at joint verification. Ops note: the blank-page gotcha recurred twice (orphan dev server + deleted `.next`); fix remains kill → clear `.next` → restart — done
- 2026-08-09 — **Phase 3 backend read endpoints built + self-verified** (design-independent, so built during the review wait). Migration 0003: `order.receipt_serial` from `receipt_serial_seq`, **allocated at settle** (not at insert) so only settled orders carry a receipt and NULL answers "is there a receipt?"; existing 9 settled orders backfilled 1–9. Seed step 7: **365 days × 2 assets of synthetic price history**, deterministic (seeded LCG, no `Math.random`), labelled `source='seed-backfill'`, anchored on the *oldest* real tick so the synthetic series ends 2026-08-05 exactly where the live feed begins 2026-08-06 — no overlap, no seam. New endpoints: `GET /portfolio` (live value, **weighted-average cost basis replayed from settled orders** — fees included, sells retire cost proportionally — gain/loss, gemstone tier + band progress, refreshes the `users.holding_tier` cache), `GET /orders` (keyset pagination on created_at, not OFFSET), `GET /orders/:id/receipt` (full breakdown + **price provenance**: feed, FX source, exact tick timestamp). **Verified live:** cost basis matched a hand replay to the cent (16,077,130); all 4 chart ranges return data (1y = 365 points); pagination cursor produces no overlap; receipt on a rejected order → 404; cross-user receipt → 404; fresh user → zeros with null percentages, no divide-by-zero; live buy settled → serial ALK-2026-000010, portfolio cost basis incremented exactly. **Money-core regression clean:** zero-sum 0 on all 3 assets, no zero-amount entries, append-only trigger still rejects UPDATE, serials unique and settled-only. Whole-workspace typecheck clean. **Also fixed:** the design brief used *estimated* prices (14,206.50 ETB/g) — the live feed says **22,525.85**; every figure in `design/` re-derived from real data and re-pushed (my own "no invented data" rule) — done
- 2026-08-09 — **Ramp v2 pushed** (commit 4067b1c, logged retroactively). Side-by-side review against the two inspiration jpgs found the surface ladder inverted: v1 wells were *lighter* than their panel, so a fee breakdown read as a raised chip instead of a sunk field. Fixed by making `--well` darker than the page ground (`oklch(0.110 0.009 78)`) plus an inner top shade, warming the whole ramp (chroma 0.012–0.030 @ hue 78–82), doubling border chroma so hairlines survive on a warm panel, and extending the CTA to four stops with a near-cream top — the client's `#d4a017`/`#b8860b` stay anchored at 72% and 100% so the falloff never dilutes the identity. 19/19 pairs recomputed and pass. `design.md` + all 7 previews pushed to ALKEVA Design System (plan `plan_d4c89daff7ea4c39_29b7ff054d74`) — done
- 2026-08-10 — **Part C: the approved comp implemented.** Imported `ALKEVA Screens.dc.html` from claude-design project `9788efcb…` — full 1440×900 frames + 390 variants for Home / Trade / Portfolio / Receipt plus a Foundations specimen, all on the ramp-v2 tokens verbatim. Built: ramp v2 into `globals.css` with three new utilities (`.well` sunk surface, `.cta-gold` four-stop glossy primary with hover/active/disabled, `.pill-active` selected state) and a `@media print` block; responsive shell (`shell/sidebar.tsx` 248px rail with two labelled sections + pinned tier chip, `shell/top-bar.tsx` with EAT clock + **live two-metal ticker**, `shell/mobile-header.tsx`, `shell/nav-items.tsx` as the single nav vocabulary, `shell/page-title.ts`) — the `max-w-[480px]` phone column is gone; `lib/use-price-series.ts` deriving real open/high/low/current/24h-change from stored ticks; `price-chart.tsx` rebuilt with a **labelled price axis on nice 1/2/5 steps**, the current price in a gold tag on that axis, a time axis and the OHLC row; Home as a 12-column dashboard with recent activity; **`/trade` three-panel workspace** mounting the same `useTradeForm` as the phone sheet; Portfolio with allocation bar + holdings table + tier card + vault strip; History as a table ≥lg and a two-line list below; **`/receipts`**; the receipt rebuilt as a centred 640px document with print + share and a print stylesheet; auth panelised on desktop. i18n: 46 new keys in both locales, Amharic taken from the comp. **Verified:** `verify-contrast.mjs` updated to the v2 tokens and the pairs the UI actually uses — **24/24 PASS** (worst 5.12:1; white-on-gold 2.38:1 correctly banned; hairline-vs-panel reported at 1.30:1); whole-workspace typecheck clean; production build clean (10 routes, 151 kB worst first load); all 7 authenticated routes 200 in **both locales** with no missing-message or render errors; served CSS confirmed carrying the v2 ramp, `.well`, `.cta-gold`, `.pill-active` and the print block. **Money core re-driven through the new UI's proxy path:** faucet → quote → settle (2 g gold, serial 13) → insufficient_metal refusal recorded; zero-sum **0** on all three assets, no zero-amount entries, append-only trigger still rejects UPDATE. `docs/manual-tests/phase-3.md` extended with **section M** (13 desktop-workspace steps) and **section N** (a 10-beat demo run sheet for the video). **Not done:** no visual pass in a real browser this session (Chrome MCP still disconnected) — sections M/N are the gate — done
- 2026-08-11 — **Phase 4 built + self-verified to the no-keys limit** (Parts A+B of the approved joint Phase 4+5 plan). Migration 0004 (payout destination cols + idempotency key, KYC bytea storage, `price_alert` table, one-time tier-1 grandfather); `system:payout_hold` account; staff seeds (compliance/finance, env-gated); env additions (Chapa/Gemini/SMTP/mins/channels) in schema + `.env.example` + `render.yaml`; `rawBody` in main.ts; `ChapaService` (initialize/verify/transfer/verify-transfer/banks-cached/balances, both signature headers timing-safe); `payments/` (initialize → webhook → **server-side verify → single idempotent credit path**; reconcile for the webhook-free local path; amount/currency/mode mismatch → audit, never credit), `payouts/` (insert-first hold, approve→transfer→verified settle, reject→refund, **ambiguity never refunds**), `kyc/` (multipart ≤2 MB, review flips tier), `notifications/` (rows + nodemailer best-effort), `webhooks/` (`@SkipThrottle`, signature-gated, 200-always after auth). Web: `/deposit`, `/deposit/return` (polls + reconciles, never credits), `/withdraw` (banks dropdown, hold explanation, request list), `/kyc` (camera capture); Home deposit/withdraw buttons; Account rows; `kyc_required` in both locales + KNOWN_ERRORS. **Self-verified live:** 503 `payments_unconfigured` (keys empty); unsigned/tampered/garbage webhook → 401, both valid signature forms → 200; KYC upload → pending → duplicate 409 → 3 MB 413; tier-0 order → `kyc_required` recorded; tier-1 buy settles; payout hold 300,000 cents posted (balance 20,000,000→19,700,000), same-key replay returns same row, over-balance `insufficient_balance` recorded; zero-sum **0** all assets, no zero-amount entries, `payout_hold` balance exact, `ledger_transaction.payout_id` linked. **Blocked on Dagmfre pasting `CHAPA_SECRET_KEY`/`CHAPA_WEBHOOK_HASH`/`GEMINI_API_KEY` into `.env`** (all three empty): real checkout→credit, approve→transfer, banks list, AI. Whole-workspace typecheck clean — done
- 2026-08-11 — **Phase 5 built + self-verified** (Parts C+D+E). **Admin console**: `admin/` API module — overview counts, user search/detail (read-only balance projections), freeze/unfreeze (compliance; reason mandatory, staff unfreezable, self-freeze forbidden), KYC queue + file streaming, payout queue (finance), **review-queue exit** (`OrdersService.resolveReview`: approve settles at the original quote's figures with EVERY money gate re-run inside the settle tx — the reserve gate holds at settle time; gate failure becomes a recorded rejection surfaced to the officer as the same machine code), transaction search, treasury detail (+ Chapa merchant balance, 60s cached), keyset-paginated audit viewer, AML CSV export — **no balance-adjust endpoint exists, verifiably**; `RolesGuard` on every route. Web: `(admin)/admin` route group, own rail (role-filtered), two-step confirm buttons, 9 screens, bilingual. **AI**: `ai/` module on `@google/genai` **Interactions API, stateless** (`store:false`, full history replayed from our own `ai_message` rows — free-tier Google retention is 1 day, never leaned on); exactly 3 read-only tools (portfolio/price/history), userId bound from the JWT, tool results pre-formatted so the model quotes rather than computes; guardrail prompt refuses advice + injects the live freeze reason; AiModule's import graph physically cannot reach a money-writing service; `/assistant` chat UI + header entry. **Alerts + notices**: `price_alert` endpoints + Home dialog + Account list; the price worker fires thresholds deterministically (conditional-UPDATE one-shot mutex) and writes the notification + optional SMTP email; bell dropdown in both shells. **Self-verified live:** roles matrix 4 roles × 8 endpoint groups exact; KYC approve→tier flip→409 double; freeze blocks faucet/orders, finance cannot unfreeze, unfreeze restores; reviews: approve settled 22g to the cent (78,540,854→27,491,919), second approve refused `insufficient_balance` by the re-run gates, third rejected `compliance_rejected`; payout reject refunded exactly (+300,000, hold back to 0), unconfigured approve left status `requested` (503, never stranded); alert fired exactly once across two ticks; all notification templates recorded (`queued` — SMTP unset by design); AI honest 503 `ai_unconfigured`; zero-sum 0/0/0, append-only trigger firing, 18/18 unique settled serials. `docs/manual-tests/phase-4.md` + `phase-5.md` written (incl. **demo run sheet N2**, superseding phase-3 §N). **Deferred:** production web build (Dagmfre's dev server owned `.next` all session — the build-vs-dev gotcha; run with dev stopped) and the **[keys]** live paths (Chapa checkout→credit, transfer approve, banks list, Gemini chat) — blocked on the three empty `.env` secrets. Ops note: ~6 stale worker processes + old dev servers from prior sessions are still running and double-writing price ticks — worth a cleanup before demo day (left alone: user-owned) — done
- 2026-08-12 — **Phase 4+5 landed on `main` and pushed; three deploy-path bugs fixed.** Commit `05a8df1` (96 files, GPG-signed) plus the 11 earlier unpushed commits — Phases 2→5 had never reached GitHub, so this deploy applies migrations 0002–0004 to production at once. **(1) `/kyc` 500'd** on the only *value* import from `@alkeva/shared` in the web app (`KYC_DOC_TYPES`): the package barrel re-exports the Node-only `env.ts`, which webpack cannot resolve for the browser — the same failure as `countdown-ring.tsx` in Phase 3. Doc types are now local and exhaustiveness-checked via `satisfies Record<KycDocType, number>`; all 33 remaining shared imports in web are `import type` (erased), and the unresolvable barrel means this class fails the build loudly rather than shipping server env code to the browser. **(2) Render build failed** — `engines.node: ">=22"` is unbounded, and Render resolves unbounded ranges to Node's *latest* (26.7.0), a read-only system install where `corepack enable` dies with `EROFS` on `/usr/bin/pnpm`; earlier deploys happened to land on Node 22, which installs project-local. Pinned with a repo-root `.node-version` (the `NODE_VERSION` env in `render.yaml` only reaches a service on a blueprint sync, and this one never got it) + upper-bounded engines. **(3) `/admin/kyc` bounced to Home** — not a gate bug: the staff seed was create-only, so `compliance@alkeva.local` kept a password known only to the Phase 5 verification session, and adding `SEED_COMPLIANCE_*` later would have been silently ignored forever (staff have no self-serve reset; on Render the seed runs every deploy). Seed now resets password/role/tier from the env pair for existing staff. **Verified:** compliance login → `/admin/kyc` 200 with no build errors, role matrix through the API exact (overview/kyc/reviews 200, payouts/treasury 403); db typecheck clean. **Render still needs by hand** (blueprint drift): `CHAPA_SECRET_KEY`, `CHAPA_WEBHOOK_HASH`, `WEB_ORIGIN` (else `return_url` points at localhost), `GEMINI_API_KEY`, and `API_URL` on the web service; Chapa webhook URL is `https://alkeva-api.onrender.com/webhooks/chapa` — done
- 2026-08-13 — **Phase 6: production-readiness pass built + self-verified live** (plan approved via AskUserQuestion: SSE push · instant-below-threshold withdrawals · administrator superuser). **WS1 one price everywhere:** worker `redis.publish` per tick → `PriceFeedService` (duplicated subscriber conn, 300ms debounce, 25s heartbeat, 90s safety poll) → `@Sse /prices/stream` + `/prices/snapshot`; canonical `change24hPctMilli` on `/prices/latest` (raw-tick vs ≥24h-old reference); web `PriceProvider` mounted in the app layout (SSE → 10s-poll fallback → 60s SSE retry, visibility pause, client staleness ratchet) + Next shadow route `/api/prices/stream` piping the upstream ReadableStream (rewrites don't guarantee SSE streaming); ticker/cards/chart-tag/trade-estimate/alert-prefill/portfolio all read the store; `usePriceSeries` lost `current`/`changePct` (renamed `lastBucket`) and 7d/30d/1y now refresh (never did). **WS2 AI:** `classifyGeminiError` + turn state machine (429→`ai_rate_limited`+retryAfterSeconds; invalid history→text-only degrade retry; transient→1.5s retry), sanitized replay with 6-row steps window + 50k-char budget, `get_price` now returns the canonical 24h change (and drops the duplicate range figure for 24h); web: pending/failed bubble states with Retry/Dismiss, New-conversation, 429→localized. **WS3 instant payouts:** `executeTransfer` extracted with `Actor` labels; auto-approve below `PAYOUT_AUTO_APPROVE_MAX_CENTS` as `system:auto_approve`. **WS4:** administrator added to all admin routes/nav/screen gates. **WS5:** `/admin/analytics` + recharts overview (stat row + 3 charts, both locales). **WS6:** worker `pg_try_advisory_lock` single-writer guard (second instance verified idling), unmapped 429s → `rate_limited` code + strings. **Three real bugs found by live verification:** Chapa transfer reference 37 chars > 36 limit (every real transfer would have failed on stage); test-mode transfers/verify returns `data:[null]` so no sandbox transfer could ever settle (now settles on envelope success, audited `sandbox_transfer_verified`); Date params crash postgres.js raw SQL (analytics 500) + zero-fill dropped today's rows (totals now match SQL to the cent: 19 orders / 148,436,941 cents / 16 users). **Verified live:** SSE initial+heartbeat+push-on-tick (curl and through the Next origin); instant withdrawal 250 ETB settled in 2.3s via real sandbox transfer, idempotent replay, exact balance delta, audit labels exact; signed `payout.success` webhook settled a parked payout; AI happy path + multi-turn quote the identical latest tick (22,802.02 ≡ ticker); admin role matrix (administrator 200 on kyc/payouts/analytics); both locales 200 on all routes on the **production build** (25 routes); zero-sum 0/0/0, no zero-amount entries; whole-workspace typecheck + prod build clean. `docs/manual-tests/phase-6.md` written (32 steps A–F). Residual 77,000-cent payout_hold balance is a genuinely `requested` pre-Phase-6 payout awaiting finance — correct, not a leak — done
- 2026-08-12 — **Admin gate no longer bounces silently.** `AdminShell` redirected a signed-in non-staff visitor to `/` with `router.replace`, which is indistinguishable from a broken route — it was reported as "/admin/kyc reroutes me to localhost:3000" and cost a diagnosis cycle. It now renders a staff-console notice naming the identity the browser actually holds ("You are signed in as {email} ({role})") plus a sign-out action — a sign-*in* link would loop, since middleware bounces signed-in visitors away from `/login`. The API's `RolesGuard` remains the only real boundary, so showing the reason leaks nothing. Verified: compliance session loads `/admin/kyc` 200 with the role matrix exact (overview/kyc/reviews 200, payouts/treasury 403); a fresh non-staff session gets the notice, not a redirect, with the Amharic string resolving under `lang="am"`; web typecheck clean, both message files valid JSON — done
